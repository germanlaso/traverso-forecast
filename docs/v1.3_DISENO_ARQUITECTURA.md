# Traverso S.A. — Diseño de arquitectura v1.3
## Cascada Lot Sizing → Sequencing con setups SKU-dependientes

**Fecha de cierre de diseño**: 02/05/2026
**Estado del backend al iniciar v1.3**: `v1.2-piloto` (tag estable, retorno seguro)
**Próxima versión objetivo**: `v1.3-piloto`
**Plataforma para implementación**: Claude Code (decisión tomada esta sesión)

---

## 1. Motivación

### Problemas detectados en v1.2-piloto

Al revisar el `Detalle de Producción` de la v1.2-piloto se identificaron tres comportamientos no deseados, todos con la misma raíz arquitectónica:

1. **Numeración de OF compartida entre días**: una misma OFT (ej. `OFT-2026-22392`) aparecía en lunes y martes simultáneamente. La regla de negocio dice que **una OF no puede quedar inconclusa al cerrar la jornada**, por lo tanto cada día con producción de un SKU debe ser una OF distinta con número propio.

2. **Subestimación de setups en días multi-SKU**: cuando un SKU producido al final del día anterior continúa en el día siguiente, ese SKU no paga setup (correcto). Pero si en el día siguiente además se produce un segundo SKU, ese segundo SKU **sí debería pagar setup**, y el modelo v1.2 no lo cobra.

3. **Falta de secuenciamiento intra-día**: el modelo v1.2 es agregado por día — decide cuántas cajas de cada SKU se producen, pero no en qué orden. Esto impide modelar correctamente el costo de cada cambio cuando hay 2-3 SKUs en un día.

### Restricción adicional próxima

En 2 semanas estará disponible una **matriz de tiempos de setup SKU→SKU**, donde el costo de cambiar de SKU A a SKU B depende del par específico. La arquitectura v1.3 debe estar lista para consumir esta matriz sin rediseño.

### Por qué no es un parche

Sin sequencing intra-día, el plan generado puede ser **infactible en planta**: la suma de tiempos de producción + setups reales puede exceder las horas disponibles del día. Cuando llegue la matriz SKU→SKU, esto se hace evidente. Por lo tanto el sequencing no es opcional.

---

## 2. Decisión arquitectónica: cascada en dos niveles

### Visión general

```
FORECAST (Prophet) → demanda diaria por SKU
        ↓
NIVEL 1 — LOT SIZING (CP-SAT, horizonte 42 días)
        Decide: cajas[día, sku, línea]
        Setup: estimación con t_cambio_hrs (predecesor anónimo)
        Restricción: Σ_k asig[d,k,l] ≤ N_max
        ↓
NIVEL 2 — SEQUENCING (CP-SAT pequeño, por línea-día)
        Para cada (línea, día) con N SKUs asignados (N≤4):
          Decide: orden de producción
          Setup real: matriz[sku_prev, sku_actual, línea]
          Restricción: tiempos respetan cap_dia[línea]
          Objetivo: minimizar Σ tiempos de setup
        ↓
GENERACIÓN DE OFs
        Una OF por cada tramo intra-día → numero_of único.
        Si SKU continúa al día siguiente → OF nueva (no atraviesa medianoche).
        ↓
FRONTEND (las 4 pestañas)
```

### Por qué cascada y no monolítico

Un modelo monolítico que decida lot sizing y sequencing simultáneamente con matriz SKU→SKU explota combinatoriamente: con 471 SKUs en 4 líneas durante 42 días, el espacio de transiciones potenciales es del orden de 471² × 4 × 42 ≈ 37 millones. CP-SAT no resuelve eso en tiempos útiles.

La cascada es la heurística estándar para CLSP-SD (Capacitated Lot Sizing Problem with Sequence-Dependent Setups). Tiene un gap de optimalidad acotado y, en la práctica industrial, se mitiga con **reserva conservadora de capacidad para setups en N1** (ver §4.1).

### Tamaños esperados

- **N1**: similar a v1.2 actual (resuelve en ~60s con 10 SKUs piloto). Se mantiene la complejidad.
- **N2**: 168 sub-problemas (4 líneas × 42 días) cada uno con N≤4 SKUs. Cada sub-problema se resuelve en milisegundos. **Total: <1 segundo** para todo el horizonte.

---

## 3. Reglas de negocio cerradas en sesión

Todas las decisiones quedaron cerradas mediante diálogo iterativo. No se introducen cambios en este apartado salvo nueva conversación con el usuario.

| # | Regla | Decisión |
|---|---|---|
| **R1** | OFs no atraviesan medianoche | Cada día con producción de un SKU = una OF nueva con `numero_of` único. Si el mismo SKU sigue al día siguiente, es **otra OF**. |
| **R2** | Máximo SKUs por día por línea | `N_max = 4` (global, no por línea). Cap. y setups limitan en la práctica. |
| **R3** | OFs aprobadas en N2 | Respetan SKU + cantidad. **El N2 sí puede mover su posición intra-día** si reduce setups. |
| **R4** | Día 0 del horizonte | **Nunca paga setup**. Todos los SKUs asignados al día 0 son "gratis" (línea se asume lista). |
| **R5** | Línea sin actividad previa | Setup = 0. Aplica si una línea no produjo nada en días anteriores del horizonte. |
| **R6** | Fin de semana | **NO resetea la línea**. El lunes el predecesor es el SKU del viernes. |
| **R7** | "Último SKU de ayer" | Para días dentro del horizonte: se calcula del propio plan. Para día 0: irrelevante por R4. **No se persiste estado externo en v1.3.** |
| **R8** | Matriz dependiente de línea | PK: `(sku_desde, sku_hasta, linea)`. Misma transición puede tener distinto costo en distintas líneas. |
| **R9** | Setup en N1 mientras no hay matriz | Usa `mrp_sku_lineas.t_cambio_hrs` actual (predecesor anónimo). Cuando llegue la matriz, N1 sigue usando t_cambio (es solo aproximación), N2 usa la matriz real. |

### Consistencia de R4, R5, R6

- **R4 (día 0 = 0 setup) reemplaza** la decisión inicial Q2 del diálogo. Confirmado explícitamente al detectar la tensión con R5.
- **R5 (línea vacía = 0)** y **R6 (no resetea fin de semana)** combinadas implican que R5 casi nunca dispara: una línea está "vacía" solo si en todo el horizonte hasta ese día no produjo nada, lo cual es raro. Sirve como caso borde.

### Comportamiento deseado del optimizador (decisión previa)

> **Stock cómodo entre SS y cap_bodega (holgura razonable).**

Esto justifica:
- Mantener W_DEFICIT alto (penalizar fuerte estar bajo SS).
- Mantener W_EXCESO alto (penalizar fuerte exceder cap_bodega).
- **No introducir penalización por estar arriba del SS** dentro del rango holgado.

---

## 4. Especificación técnica

### 4.1 Nivel 1 — Lot Sizing (modificado respecto a v1.2)

**Variables existentes (sin cambio)**:
- `cajas[d,k,l]` IntVar
- `prod_u[d,k,l] = u_por_caja[k] × cajas[d,k,l]`
- `asig[d,k,l]` BoolVar
- `inicio[d,k,l]` BoolVar
- `stock_u[d,k]` IntVar
- `def_[d,k]`, `exc_[d,k]` IntVar ≥ 0

**Cambios respecto a v1.2**:

1. **Nueva restricción**: `Σ_k asig[d,k,l] ≤ N_max` para cada `(d, l)`. Con `N_max = 4`. Esto acota el problema de N2 y evita planes infactibles.

2. **Reservar capacidad de setup en R1a**: la restricción agregada de capacidad ya considera `inicio[d,k,l] × t_cambio[k,l] × velocidad`. Mantener esto. Esta reserva es **conservadora**: N2 puede pagar más o menos según la matriz real, pero N1 reserva un piso seguro.

3. **Eliminar / reducir `W_SETUP = 200`**: hoy el peso de cada `inicio` es 200 puntos. En v1.3, dado que N2 será quien optimice setups con matriz real, **N1 ya no necesita penalizar fragmentación con un escalar arbitrario**. Decisión: **bajar `W_SETUP` a un valor pequeño (e.g. 20) o eliminarlo**. Validar empíricamente en F1.

4. **Mantener W_DEFICIT, W_EXCESO, W_ALT** sin cambio.

5. **Día 0 según R4**: la restricción actual `inicio[0] = asig[0]` debe **eliminarse** o cambiarse a `inicio[0] = 0`. Equivale a "el día 0 nunca paga setup en N1". El N2 hará lo mismo independientemente.

### 4.2 Nivel 2 — Sequencing (nuevo módulo `sequencer.py`)

**Input**: para cada `(día, línea)`, lista de SKUs asignados con sus cantidades en cajas y unidades, provenientes de N1.

**Variables CP-SAT por sub-problema**:

```
N = número de SKUs asignados ese día en esa línea (N ≤ 4)

Para cada SKU k en {1..N}:
  pos[k] ∈ {1, ..., N}        // posición en la secuencia
  paga_setup[k] BoolVar        // si paga setup este día
  tiempo_inicio[k] IntVar      // minuto del día en que arranca

AllDifferent(pos[1], ..., pos[N])
```

**Restricciones**:

1. **Conexión con día anterior**:
   - Si `día == 0`: `paga_setup[k] = 0` para todo k (R4).
   - Si línea estaba vacía hasta ayer (R5): `paga_setup[k] = 0`.
   - Si no: sea `prev = último_sku_dia_anterior(línea)`. Para el SKU con `pos = 1`:
     - Si `k == prev`: `paga_setup[k] = 0` (continúa).
     - Si `k != prev`: `paga_setup[k] = 1`.
   - Para SKUs con `pos > 1`: `paga_setup[k] = 1` (siempre pagan, vienen después de otro SKU del mismo día).

2. **Tiempo total** (cabe en horas del día):
   ```
   Σ_k (cajas[k] × u_por_caja[k] / velocidad_línea_efectiva[k]) +
   Σ_k paga_setup[k] × matriz[predecesor[k], k, línea] ≤ horas_dia[línea]
   ```
   Donde `predecesor[k]` es el SKU en posición `pos[k] - 1` (si `pos[k] > 1`) o el último de ayer (si `pos[k] = 1`).

3. **OFs aprobadas (R3)**:
   - `cantidad_cajas[k]` queda fijo (no es variable, viene de N1 + aprobación).
   - `pos[k]` sí es variable: el N2 puede optar por moverla si reduce setups.

**Objetivo**:

```
min Σ_k paga_setup[k] × matriz[predecesor[k], k, línea]
```

**Predecesor variable**: el cálculo de `predecesor[k]` requiere encadenar `pos[k]` con la secuencia. Esto se modela con **variables intermedias** o con la formulación clásica TSP-like (ver §6 — Detalles de implementación).

**Estado entre días**: el "último SKU del día anterior" se calcula iterativamente:
- Día 0: por R4, no importa.
- Día 1+: del propio resultado del N2 del día anterior — el SKU con `pos[k] = N` en esa línea.

Por eso N2 procesa los días **en orden cronológico**, no en paralelo.

### 4.3 Generación de OFs

Una vez secuenciado el día por línea, cada SKU produce **una sola OF**:

```python
OF = {
  "numero_of": <correlativo único nuevo>,
  "fecha": <ese día específico>,
  "linea": <esa línea>,
  "sku": <SKU>,
  "cantidad_cajas": <cajas asignadas ese día>,
  "cantidad_unidades": <equivalente en unidades>,
  "posicion_en_dia": <pos[k] desde N2>,        # nueva
  "paga_setup": <paga_setup[k] desde N2>,
  "setup_unidades": <int(matriz[prev,k,linea] × velocidad) si paga, 0 sino>,
  "predecesor_sku": <SKU previo si paga_setup>,  # nueva, útil para UI
  ...
}
```

Cuando el mismo SKU continúa al día siguiente (R1), se genera **otra OF** con `numero_of` distinto.

---

## 5. Cambios en BD

### 5.1 Tabla nueva `mrp_setup_matrix`

```sql
CREATE TABLE mrp_setup_matrix (
    sku_desde       VARCHAR(30) NOT NULL,
    sku_hasta       VARCHAR(30) NOT NULL,
    linea           VARCHAR(20) NOT NULL,
    tiempo_horas    FLOAT NOT NULL,
    updated_at      TIMESTAMP DEFAULT NOW(),
    PRIMARY KEY (sku_desde, sku_hasta, linea)
);

CREATE INDEX idx_setup_matrix_linea ON mrp_setup_matrix(linea);
```

**Migración inicial**: poblar la matriz con valores tomados de `mrp_sku_lineas.t_cambio_hrs`, asumiendo que el `t_cambio` actual representa el "cambio anónimo promedio" hacia ese SKU desde cualquier predecesor. Es decir:

```sql
-- Pseudocódigo: para cada par (sku_desde, sku_hasta, línea) donde ambos SKUs
-- están en mrp_sku_lineas para esa línea:
--   tiempo_horas = mrp_sku_lineas.t_cambio_hrs[sku_hasta, línea]
```

Esto es una aproximación inicial. Cuando el equipo de producción entregue la matriz real, se carga vía endpoint `POST /params/setup-matrix/importar` (Excel/CSV).

### 5.2 NO se crea tabla de estado de línea

Decisión: el estado se reconstruye del propio plan (R7). En v1.3 piloto basta. En el futuro, si se integra con datos reales de planta, se evalúa.

---

## 6. Cambios en endpoints

### Endpoints modificados

- **`POST /plan`**: internamente ahora hace N1 → N2 → numeración. La firma externa (request/response) se mantiene compatible. Los campos nuevos en cada OFT son aditivos: `posicion_en_dia`, `predecesor_sku`. Los campos `paga_setup`, `setup_unidades` ya existen y se respetan.

### Endpoints nuevos

- **`GET /params/setup-matrix`**: lista entradas de la matriz. Soporta query params `linea`, `sku_desde`, `sku_hasta`.
- **`PUT /params/setup-matrix/{from}/{to}/{linea}`**: actualiza una entrada.
- **`POST /params/setup-matrix/importar`**: carga masiva desde archivo (Excel o CSV).
- **`DELETE /params/setup-matrix/{from}/{to}/{linea}`**: elimina entrada (cae a default si existe, sino error en N2).

---

## 7. Cambios en frontend

Mínimos pero importantes para reflejar la nueva semántica. Detalle en F6.

- **`DetalleProduccion.jsx`**:
  - Cada badge ahora corresponde a una OF única. No se repiten números entre días.
  - Los SKUs aparecen ordenados intra-día según `posicion_en_dia` (1, 2, 3, ...) en lugar de orden arbitrario actual.
  - Tooltip enriquecido: además de SKU + cajas + setup, muestra `predecesor_sku` cuando paga setup.

- **`Plan de Producción`**: cada OFT ya tiene fecha exacta (de v1.2). Sin cambios estructurales. Eventualmente añadir columna `posicion_en_dia` si el usuario lo pide.

- **`StockProyeccion`**: sin cambios (ya consume datos del backend correctamente).

- **`App.js`**: sin cambios estructurales.

---

## 8. Plan de fases

| Fase | Alcance | Archivos clave | Bloqueantes | Estimación |
|---|---|---|---|---|
| **F1** | N1 ajustado: agregar `Σ asig ≤ 4`, ajustar `W_SETUP`, día 0 sin setup. Mantener todo lo demás. | `optimizer.py` | Ninguno | 1 sesión |
| **F2** | Implementar N2 con matriz dummy (uniforme = `t_cambio_hrs`). Integración en `/plan` después de N1. | `sequencer.py` (nuevo), `main.py` | F1 | 1-2 sesiones |
| **F3** | Refactor de numeración: PK por `(sku, fecha_lanzamiento, linea)` no por `(sku, semana_emision)`. | `main.py` | F1, F2 | 0.5 sesión |
| **F4** | Tabla `mrp_setup_matrix` + endpoints CRUD + importar Excel. Migración con valores actuales. | `db_mrp.py`, `main.py`, schema PG | Ninguno (paralelo con F1-F3) | 1 sesión |
| **F5** | Cuando llegue la matriz real: carga, verificación, conexión final con N2. | Datos en BD | F4 + matriz disponible | 0.5 sesión |
| **F6** | Frontend: orden intra-día, tooltip enriquecido, validación visual. | `DetalleProduccion.jsx` y otros | F1-F3 | 1 sesión |

**Hito v1.3-piloto**: F1 + F2 + F3 + F6 completos. La matriz real (F5) puede llegar después y solo cambia los números, no la arquitectura.

**Tiempo total estimado**: 4-5 sesiones de Claude Code.

---

## 9. Riesgos y mitigaciones

| Riesgo | Probabilidad | Impacto | Mitigación |
|---|---|---|---|
| Gap de optimalidad N1↔N2: N1 asigna 3 SKUs a un día donde N2 no logra acomodar setups reales | Media | Alto (plan infactible) | Reserva conservadora de capacidad para setup en N1 (sobreestimar t_cambio si dudoso). Si N2 detecta infactibilidad, aviso visual en `alerta` y/o re-optimización en cascada. |
| Matriz dummy inicial subestima setups reales | Alta (es el peor caso) | Medio (planes optimistas hasta F5) | Aceptable. F5 corrige al cargar la matriz real. |
| Performance de N2 al escalar a 471 SKUs | Baja (sub-problemas independientes y pequeños) | Bajo | N2 sigue siendo 168 sub-problemas con N≤4 SKUs cada uno. Independiente del total de SKUs. |
| Performance de N1 al escalar a 471 SKUs | Alta | Alto | No es problema de v1.3 sino de escalamiento posterior. Posibles soluciones: rolling horizon, agrupación por categoría, warm starting. Tema para v1.4. |
| Cambio de numeración rompe vista actual | Media | Medio | F3 es atómica: cambio en `main.py`, una sesión. Si rompe, rollback fácil con tag git. |
| OFs aprobadas con posición vieja al regenerar plan | Baja | Bajo | R3: N2 respeta cantidad, mueve posición. Si genera diferencia visible, el usuario la verá y puede rechazarla. |

---

## 10. Tag de retorno seguro

Cualquier momento durante v1.3, si algo se rompe sin solución clara:

```bash
git checkout v1.2-piloto
docker compose down && docker compose up -d
docker exec traverso_forecast pip install reportlab==4.1.0 ortools "numpy<2.0" "pandas<2.0" --break-system-packages -q --force-reinstall
docker exec traverso_forecast rm -rf /app/__pycache__
docker compose restart forecast
```

Esto deja el sistema operativo en el estado validado al 01/05/2026.

---

## 11. Referencias

- **Estado técnico previo**: `ESTADO_TECNICO_PROYECTO_01-05-26.md` (v1.2-piloto, snapshot del 01/05/2026).
- **Documento de avance ejecutivo**: `Traverso_Avance_Proyecto_v5.pdf` (presentación al negocio).
- **Guía Optimizador OR-Tools**: `Traverso_Optimizador_ORTools_Guia.pdf` (modelo v1.2, base para v1.3).
- **Optimizador v1.2 actual**: `forecast/optimizer.py` (~1.100 líneas, base para F1).
- **Frontend v1.2 actual**: `dashboard/src/components/DetalleProduccion.jsx` (~646 líneas, base para F6).

---

## 12. Onboarding para Claude Code

El archivo `CLAUDE.md` en la raíz del repo es el punto de entrada para sesiones de Claude Code. Contiene:

- Stack del proyecto y arquitectura.
- Comandos típicos (Docker, PostgreSQL, sincronización local↔contenedor).
- Convenciones de código.
- Punteros a este documento de diseño v1.3.
- Estado actual y fase activa.

Al iniciar Claude Code en la raíz del repo:

```bash
cd C:\proyectos\traverso  # o donde tengas el repo
claude
```

Primer mensaje sugerido:
> Lee `CLAUDE.md` y `docs/v1.3_DISENO_ARQUITECTURA.md`. Confirma qué entiendes del proyecto y de las decisiones de v1.3 cerradas. Empecemos por F1: ajuste de Nivel 1 en `optimizer.py`.
